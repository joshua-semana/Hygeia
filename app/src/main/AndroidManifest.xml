<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hygeia"
        tools:targetApi="31" >

        <activity
            android:name=".ActRequestMoney"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ActSendMoney"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ActForgotPassword"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ActTransactions" />
        <activity android:name=".ActNotifications" />
        <activity android:name=".ActTopUp" />
        <activity android:name=".ActMain" />
        <activity android:name=".ActCreateAccount" />
        <activity android:name=".ActLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>